using Application.Contracts.Helpers;

namespace Application.Helpers;
internal class CommonHelpers : ICommonHelpers
{
    //private decimal RoundedDistance(int value, double dividedBy)
    //{
    //    return decimal.Round(Convert.ToDecimal(value / dividedBy), MidpointRounding.AwayFromZero);
    //}

    //private bool InRange(int low, int high, int value)
    //{
    //    return value >= low && value <= high;
    //}
    
}
